# To change this license header, choose License Headers in Project Properties.
# To change this template file, choose Tools | Templates
# and open the template in the editor.

deleteCard = DELETE FROM cards WHERE cards.card_id = ?

getCard = SELECT * FROM cards WHERE card_id = ? LIMIT 1

getCards = SELECT * FROM cards WHERE column_id = ? 

updateCard = UPDATE cards SET card_name = ?, card_description = ? WHERE cards.card_id = ?

registerCard = INSERT INTO cards (card_id, column_id, user_id, card_name, card_description) VALUES (NULL, ? , ? ,  ? , ?);

